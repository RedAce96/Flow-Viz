schema_version: 1
analyses:
  - {id: packet, recipe: transient_wavepacket, probe_set_id: default, variable: pressure, probe_indices: [0, 1, 2, 3], band_min_hz: 1.0e6, band_max_hz: 5.0e7, baseline_end_time_s: 5.0e-8, probe_plotting: {mode: both, normalization: none, label: index_coordinates}}
